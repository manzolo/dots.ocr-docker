# dots.ocr - CPU-only mode (no GPU required)
# Much slower than GPU, but works on any machine
#
# First build the CPU image:  ./dots-ocr-manager.sh build-cpu
# Then start with:            ./dots-ocr-manager.sh up --cpu

VLLM_CPU_IMAGE=dots-ocr-cpu:v0.11.1
OCR_MODEL=rednote-hilab/dots.ocr
API_PORT=8000
VLLM_TOKEN=your-secret-token-here

MAX_MODEL_LEN=4096
MAX_IMAGES_PER_PROMPT=6
